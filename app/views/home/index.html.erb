<!--This doesn't work  - passes props, but inside Component {this.props.key} is undefined -->
<%= # react_component('Shopify',
    #                props: {
    #                    key: @shopify_key,
    #                    domain: @shopify_domain
    #                }
    #)
%>
